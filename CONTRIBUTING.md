# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту Social Post Generator! Мы рады любому вкладу.

## 📋 Как внести вклад

### 1. Fork репозитория

Создайте fork репозитория на GitHub.

### 2. Клонируйте ваш fork

```bash
git clone https://github.com/ваш-username/social_post_generator.git
cd social_post_generator
```

### 3. Создайте ветку для изменений

```bash
git checkout -b feature/ваша-фича
# или
git checkout -b fix/ваш-фикс
```

### 4. Настройте окружение разработки

```bash
# Создайте виртуальное окружение
python -m venv .venv
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Настройте .env (получите ProxyAPI ключ)
```

### 5. Внесите изменения

- Пишите чистый, читаемый код
- Следуйте стилю существующего кода
- Добавляйте комментарии на русском языке
- Обновляйте документацию при необходимости

### 6. Протестируйте изменения

```bash
# Запустите приложение
python app.py

# Протестируйте функциональность
# Убедитесь что ничего не сломалось
```

### 7. Commit и Push

```bash
git add .
git commit -m "Описание изменений"
git push origin feature/ваша-фича
```

### 8. Создайте Pull Request

- Перейдите на GitHub
- Создайте Pull Request из вашей ветки в main
- Опишите что изменено и зачем

## 📝 Стандарты кода

### Python

- **Стиль**: PEP 8
- **Комментарии**: На русском языке
- **Docstrings**: Для всех классов и функций
- **Типы**: Type hints где возможно

**Пример:**

```python
def generate_post(self, url: str, style: Optional[str] = None) -> str:
    """
    Сгенерировать пост на основе контента URL.
    
    Args:
        url: URL страницы
        style: Стиль генерации (опционально)
        
    Returns:
        str: Сгенерированный пост
    """
    pass
```

### JavaScript

- **Стиль**: ES6+
- **Комментарии**: На русском языке
- **Naming**: camelCase для переменных и функций

### Git Commits

- Пишите осмысленные сообщения
- Используйте императив: "Добавить", "Исправить", "Обновить"
- Можно на русском или английском

**Примеры:**
- ✅ `Добавить поддержку новых стилей генерации`
- ✅ `Исправить ошибку валидации URL`
- ✅ `Обновить документацию API`
- ❌ `fix` (слишком кратко)
- ❌ `asdfasdf` (бессмысленно)

## 🐛 Баг репорты

При обнаружении бага создайте Issue с:

- **Описанием проблемы**
- **Шагами для воспроизведения**
- **Ожидаемым поведением**
- **Фактическим поведением**
- **Логами** (если есть)
- **Версией** (Python, OS)

## 💡 Предложения

Для предложения новых функций создайте Issue с описанием:

- Что хотите добавить
- Зачем это нужно
- Примеры использования

## ✅ Чек-лист перед Pull Request

- [ ] Код работает локально
- [ ] Нет ошибок в логах
- [ ] Документация обновлена (если нужно)
- [ ] Комментарии добавлены
- [ ] `.env.example` обновлен (если добавлены новые параметры)
- [ ] Commit сообщения осмысленные

## 🚫 Что НЕ принимается

- Код без комментариев
- Нарушение стиля проекта
- Изменения без описания
- Секреты и API ключи в коде
- Большие бинарные файлы

## 📞 Вопросы?

Если что-то непонятно:
- Создайте Issue с вопросом
- Напишите на support@example.com
- Изучите существующий код как пример

---

**Спасибо за вклад в проект! 🎉**

