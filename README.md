# 🎯 Social Post Generator

**Мощный AI-генератор постов для социальных сетей**

Автоматически создавайте захватывающие посты на основе контента любых веб-страниц с использованием искусственного интеллекта GPT.

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.10+-green.svg)
![License](https://img.shields.io/badge/license-MIT-purple.svg)
![Docker](https://img.shields.io/badge/docker-ready-blue.svg)
![ProxyAPI](https://img.shields.io/badge/ProxyAPI-supported-success.svg)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Демо](#-демо)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [API документация](#-api-документация)
- [Разработка](#-разработка)
- [Docker](#-docker)
- [Troubleshooting](#-troubleshooting)
- [FAQ](#-faq)
- [Лицензия](#-лицензия)

---

## ✨ Возможности

### 🤖 AI-генерация
- **6 уникальных стилей**: Ироничный, Профессиональный, Мотивационный, Юмористический, Образовательный, Эмоциональный
- **Умная обработка контента**: Автоматическое извлечение ключевой информации из статей
- **GPT-4o / GPT-4.1**: Использование передовых языковых моделей OpenAI через ProxyAPI.ru
- **Без VPN**: Работает из России через ProxyAPI.ru

### 🎨 Современный интерфейс
- **Красивый дизайн**: Градиенты, анимации, темная тема
- **Responsive**: Отлично работает на мобильных и десктопе
- **Интуитивный UX**: Простой и понятный интерфейс

### ⚙️ Технические возможности
- **REST API**: Полноценный API для интеграций
- **Docker-ready**: Легко разворачивается в контейнерах
- **Логирование**: Многоуровневое логирование с ротацией
- **Обработка ошибок**: Дружественные сообщения для пользователей
- **Rate limiting**: Защита от злоупотреблений

---

## 🎬 Демо

### Веб-интерфейс
```
http://localhost:8082
```

### Пример использования

**Входные данные:**
- URL: `https://habr.com/ru/articles/python`
- Стиль: Ироничный

**Результат:**
```
🐍 Python снова признан самым популярным языком! 
И знаете что? Это совсем не удивительно. 
Ведь где ещё можно так элегантно забыть поставить двоеточие 
и потратить 3 часа на поиски бага? 😅

#Python #Programming #Coding
```

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- API ключ ProxyAPI ([получить здесь](https://proxyapi.ru/)) - для доступа к OpenAI из России
- Git

> **💡 Что такое ProxyAPI?** Это сервис доступа к OpenAI API из России без VPN. Оплата в рублях. 
> Подробнее: [docs/PROXYAPI.md](docs/PROXYAPI.md)

### Установка за 3 шага

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd social_post_generator

# 2. Настройте окружение
cp .env.example .env
# Отредактируйте .env и укажите ваш OPENAI_API_KEY

# 3. Запустите через Docker
docker-compose up -d
```

Готово! 🎉 Откройте http://localhost:8082

---

## 📦 Установка

### Метод 1: Docker (Рекомендуется для продакшна)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd social_post_generator

# Создайте .env файл
cp .env.example .env
nano .env  # Укажите OPENAI_API_KEY

# Запустите контейнеры
docker-compose up -d

# Проверьте статус
docker-compose ps

# Просмотр логов
docker-compose logs -f app
```

**На продакшн сервере также рекомендуется настроить Nginx:**

```bash
# Быстрая настройка Nginx (5 минут)
sudo apt install nginx -y

# Создайте конфигурацию для вашего домена
sudo nano /etc/nginx/sites-available/social-post-generator

# См. docs/NGINX_SETUP.md для готовой конфигурации
```

📖 **Полная инструкция:** [Настройка Nginx](docs/NGINX_SETUP.md) или [Развертывание](docs/DEPLOYMENT.md#nginx-reverse-proxy)

### Метод 2: Python venv (Для разработки)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd social_post_generator

# Создайте виртуальное окружение
python -m venv .venv

# Активируйте окружение
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env

# Запустите приложение
python app.py
```

Приложение будет доступно на `http://localhost:8082`

---

## ⚙️ Конфигурация

### Основные параметры (.env)

```ini
# ProxyAPI Configuration (доступ к OpenAI из России)
OPENAI_API_KEY=your-proxyapi-key-here              # Обязательно! Получить на https://proxyapi.ru/
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1  # НЕ ИЗМЕНЯЙТЕ!
OPENAI_MODEL=gpt-4o                                 # gpt-4o, gpt-4.1-mini, gpt-5-mini

# Application Configuration
PORT=8082                                     # Порт приложения
HOST=0.0.0.0                                  # Хост (0.0.0.0 для внешнего доступа)
DEBUG=false                                   # Режим отладки
LOG_LEVEL=INFO                                # DEBUG, INFO, WARNING, ERROR, CRITICAL

# CORS (разрешенные источники через запятую)
ALLOWED_ORIGINS=http://localhost:8082,http://127.0.0.1:8082

# Rate Limiting
RATE_LIMIT_PER_MINUTE=10                      # Запросов в минуту
RATE_LIMIT_PER_HOUR=100                       # Запросов в час

# Web Scraping
FETCH_TIMEOUT=30                              # Таймаут загрузки страниц (сек)
MAX_PAGE_SIZE=5242880                         # Макс размер страницы (5 MB)

# Content Limits
MAX_POST_LENGTH=800                           # Макс длина поста
MIN_TEXT_LENGTH=100                           # Мин текста на странице
```

### Файловая структура

```
social_post_generator/
├── docs/                    # Документация
│   ├── ARCHITECTURE.md      # Архитектура системы
│   ├── IMPLEMENTATION_PLAN.md
│   └── API.md              # API спецификация
├── static/                  # Фронтенд
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── logs/                    # Логи (создается автоматически)
├── agent.py                 # Агент генерации
├── openai_module.py        # OpenAI клиент
├── app.py                   # Веб-сервер FastAPI
├── config.py               # Конфигурация
├── logger.py               # Логирование
├── exceptions.py           # Исключения
├── requirements.txt        # Зависимости Python
├── .env                    # Настройки (не в git!)
├── .env.example           # Шаблон настроек
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose
└── README.md              # Этот файл
```

---

## 💻 Использование

### Веб-интерфейс

1. Откройте http://localhost:8082
2. Вставьте URL интересной статьи
3. Выберите стиль поста
4. Нажмите "Сгенерировать пост"
5. Скопируйте результат одним кликом

### API

#### Генерация поста

```bash
curl -X POST http://localhost:8082/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/article",
    "style": "ироничный"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "post": "Сгенерированный пост...",
  "length": 234,
  "style": "ироничный",
  "url": "https://example.com/article",
  "timestamp": "2025-11-01T12:00:00Z"
}
```

#### Список стилей

```bash
curl http://localhost:8082/api/styles
```

#### Проверка здоровья

```bash
curl http://localhost:8082/api/health
```

### Python SDK (пример)

```python
import requests

# Генерация поста
response = requests.post(
    'http://localhost:8082/api/generate',
    json={
        'url': 'https://habr.com/ru/articles/python',
        'style': 'ироничный'
    }
)

data = response.json()
if data['success']:
    print(f"Пост: {data['post']}")
    print(f"Длина: {data['length']} символов")
else:
    print(f"Ошибка: {data['error']}")
```

---

## 📚 API документация

### Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Веб-интерфейс |
| POST | `/api/generate` | Генерация поста |
| GET | `/api/styles` | Список стилей |
| GET | `/api/health` | Health check |
| GET | `/docs` | Swagger UI |

Подробная документация: [docs/API.md](docs/API.md)

**Swagger UI:** http://localhost:8082/docs

---

## 🛠 Разработка

### Настройка окружения разработки

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd social_post_generator

# Создайте venv
python -m venv .venv
.venv\Scripts\activate  # Windows
# source .venv/bin/activate  # Linux/Mac

# Установите зависимости
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
# Установите DEBUG=true и LOG_LEVEL=DEBUG

# Запустите в режиме разработки
python app.py
```

### Структура кода

- **agent.py** - Бизнес-логика генерации постов
- **openai_module.py** - Интеграция с OpenAI API
- **app.py** - FastAPI веб-сервер
- **config.py** - Загрузка конфигурации
- **logger.py** - Система логирования
- **exceptions.py** - Кастомные исключения

### Тестирование модулей

```bash
# Тест конфигурации
python config.py

# Тест логирования
python logger.py

# Тест OpenAI клиента
python openai_module.py

# Тест агента
python agent.py

# Тест исключений
python exceptions.py
```

### Логи

Логи сохраняются в директории `logs/`:
- `logs/app.log` - основной лог
- `logs/error.log` - только ошибки

В режиме DEBUG логи максимально детальные.

---

## 🐳 Docker

### Сборка образа

```bash
# Сборка
docker build -t social-post-generator .

# Запуск
docker run -d \
  --name social_post_gen \
  -p 8082:8082 \
  --env-file .env \
  social-post-generator
```

### Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down

# Пересборка при изменениях
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f app

# Перезапуск
docker-compose restart app
```

### Обновление на VPS

```bash
# SSH на сервер
ssh user@your-vps.com

# Переход в директорию проекта
cd /path/to/social_post_generator

# Получение изменений
git pull

# Пересборка и перезапуск
docker-compose down
docker-compose up -d --build

# Проверка
docker-compose ps
docker-compose logs -f app
```

---

## 🔧 Troubleshooting

### Проблема: "OPENAI_API_KEY не установлен"

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep OPENAI_API_KEY

# Если пустой - добавьте ключ
echo "OPENAI_API_KEY=sk-your-key" >> .env

# Перезапустите приложение
docker-compose restart app
```

### Проблема: "Не удалось загрузить страницу"

**Возможные причины:**
- URL недоступен или требует авторизацию
- Сайт блокирует ботов
- Слишком большая страница

**Решение:**
- Проверьте URL в браузере
- Увеличьте `FETCH_TIMEOUT` в .env
- Увеличьте `MAX_PAGE_SIZE`

### Проблема: "Rate limit exceeded"

**Решение:**
- Подождите несколько минут
- Проверьте лимиты OpenAI
- Увеличьте `RATE_LIMIT_PER_MINUTE` в .env для своего IP

### Проблема: Порт 8082 занят

**Решение:**
```bash
# Windows
netstat -ano | findstr :8082

# Linux/Mac
lsof -i :8082

# Измените порт в .env и docker-compose.yml
PORT=8083
ports:
  - "8083:8083"
```

### Логи для диагностики

```bash
# Docker логи
docker-compose logs -f app

# Файловые логи
tail -f logs/app.log
tail -f logs/error.log

# Включить DEBUG режим
# В .env: LOG_LEVEL=DEBUG
docker-compose restart app
```

---

## ❓ FAQ

**Q: Поддерживаются ли другие языки кроме русского?**  
A: Да, GPT поддерживает многие языки. Контент страницы определит язык поста автоматически.

**Q: Какую модель выбрать?**  
A: Для продакшна - `gpt-4o`, для разработки и экономии - `gpt-4.1-mini`.

**Q: Сколько стоит генерация одного поста?**  
A: Через ProxyAPI: gpt-4o ~5-15₽, gpt-4.1-mini ~1-3₽ за пост.

**Q: Что такое ProxyAPI?**  
A: Сервис доступа к OpenAI из России без VPN. Оплата в рублях. Подробнее: [docs/PROXYAPI.md](docs/PROXYAPI.md)

**Q: Можно ли добавить свои стили?**  
A: Да, в файле `agent.py` в словаре `STYLES` можно добавить новые стили с промптами.

**Q: Как увеличить длину поста?**  
A: Измените `MAX_POST_LENGTH` в .env (максимум 800 символов по умолчанию).

**Q: Работает ли на Windows/Linux/Mac?**  
A: Да, приложение кроссплатформенное.

**Q: Нужен ли платный API ключ?**  
A: Да, нужен ProxyAPI ключ с положительным балансом. Регистрация на https://proxyapi.ru/

**Q: Можно ли использовать для коммерческих целей?**  
A: Да, проверьте условия лицензии OpenAI для коммерческого использования.

---

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE) файл.

---

## 🤝 Поддержка

- 📧 Email: support@example.com
- 💬 Telegram: @social_post_gen
- 🐛 Issues: GitHub Issues
- 📖 Документация: `/docs`
- 📡 ProxyAPI: [docs/PROXYAPI.md](docs/PROXYAPI.md)

---

## 🙏 Благодарности

- [ProxyAPI.ru](https://proxyapi.ru/) - за доступ к OpenAI в России
- [OpenAI](https://openai.com) - за GPT модели
- [FastAPI](https://fastapi.tiangolo.com) - за отличный фреймворк
- [BeautifulSoup](https://www.crummy.com/software/BeautifulSoup/) - за парсинг HTML

---

## 🚀 Roadmap

- [ ] Поддержка нескольких языков интерфейса
- [ ] История генераций
- [ ] Кэширование результатов
- [ ] Пакетная генерация
- [ ] Интеграция с соцсетями (прямой постинг)
- [ ] Аналитика популярности постов
- [ ] Мобильное приложение

См. полный список в [CHANGELOG.md](CHANGELOG.md)

---

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! 

- 📖 Прочитайте [CONTRIBUTING.md](CONTRIBUTING.md)
- 🐛 Создайте [Issue](https://github.com/your-repo/issues) если нашли баг
- 💡 Предложите новую функцию через Issue
- 🔧 Создайте Pull Request с улучшениями

---

**Сделано с ❤️ и AI**

⭐ Поставьте звезду если проект понравился!

[![GitHub stars](https://img.shields.io/github/stars/your-repo/social_post_generator?style=social)](https://github.com/your-repo/social_post_generator)

