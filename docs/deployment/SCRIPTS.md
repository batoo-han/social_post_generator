# 📜 Справка по скриптам проекта

## Доступные скрипты

### 🚀 Запуск приложения

#### `run.sh` / `run.bat`
**Быстрый запуск для разработки**

```bash
# Linux/Mac
./run.sh

# Windows
run.bat
```

**Что делает:**
- Создает `.venv` если нет
- Устанавливает зависимости
- Запускает приложение

---

### 🔄 Обновление приложения

#### `update.sh` / `update.bat` ⭐ РЕКОМЕНДУЕТСЯ
**Умное обновление с автоопределением**

```bash
# Linux/Mac
./update.sh

# Windows
update.bat
```

**Что делает:**
- ✅ Backup конфигурации и логов
- ✅ Git pull обновлений
- ✅ Анализ изменений
- ✅ Rebuild ИЛИ restart (автоматически)
- ✅ Проверка работоспособности
- ✅ Очистка старых backup

**Когда использовать:**
- Обычные обновления
- После git pull
- Регулярное обслуживание

---

#### `update-smart.sh` (только Linux)
**Продвинутая версия с откатом**

```bash
./update-smart.sh
```

**Дополнительно:**
- ✅ Детальный анализ Git diff
- ✅ Автоматический откат при ошибках
- ✅ Умная логика обновлений

**Когда использовать:**
- Продакшн сервер
- Критичные обновления
- Нужна максимальная надежность

---

### 🧹 Полная очистка и пересборка

#### `docker-rebuild.sh` / `docker-rebuild.bat`
**Принудительная полная пересборка**

```bash
# Linux/Mac
./docker-rebuild.sh

# Windows
docker-rebuild.bat
```

**Что делает:**
- 🧹 Удаляет все образы приложения
- 🧹 Очищает Docker build cache
- 🧹 Очищает dangling образы
- 🧹 Очищает директорию logs
- 🔨 Пересобирает БЕЗ кэша
- 🚀 Запускает контейнеры

**Когда использовать:**
- Проблемы с образом
- Ошибки Permission denied
- Накопился кэш
- После серьезных изменений в Dockerfile

---

## 🎯 Какой скрипт использовать?

### Ситуация → Скрипт

| Ситуация | Скрипт | Команда |
|----------|--------|---------|
| Первый запуск | `run.sh/bat` | `./run.sh` |
| Обычное обновление | `update.sh/bat` | `./update.sh` |
| Продакшн обновление | `update-smart.sh` | `./update-smart.sh` |
| Проблемы с Docker | `docker-rebuild.sh/bat` | `./docker-rebuild.sh` |
| Ошибка "Permission denied" | `docker-rebuild.sh/bat` | `./docker-rebuild.sh` |
| Накопился кэш | `docker-rebuild.sh/bat` | `./docker-rebuild.sh` |

---

## 📋 Детальное сравнение

### run.sh / run.bat

**Назначение:** Быстрый запуск для разработки  
**Скорость:** ⚡⚡⚡ Очень быстро  
**Сложность:** 🟢 Простой  
**Использование:** Локальная разработка  

**Особенности:**
- Без Docker
- Использует venv
- Идеально для тестирования изменений

---

### update.sh / update.bat

**Назначение:** Умное обновление  
**Скорость:** ⚡⚡ Быстро (если не нужен rebuild)  
**Сложность:** 🟡 Средний  
**Использование:** Продакшн и dev  

**Особенности:**
- Автоопределение типа обновления
- Backup перед обновлением
- Проверка работоспособности
- Работает на Windows и Linux

---

### update-smart.sh

**Назначение:** Максимально умное обновление  
**Скорость:** ⚡⚡ Быстро  
**Сложность:** 🟡 Средний  
**Использование:** Только продакшн  

**Особенности:**
- Детальный Git анализ
- Автоматический откат
- Только для Linux
- Лучший выбор для сервера

---

### docker-rebuild.sh / docker-rebuild.bat

**Назначение:** Полная пересборка  
**Скорость:** 🐌 Медленно (5-10 минут)  
**Сложность:** 🔴 Максимальный  
**Использование:** Решение проблем  

**Особенности:**
- Удаляет все образы
- Очищает весь кэш
- Гарантированно чистая сборка
- Используйте при проблемах

---

## 🛠️ Подготовка скриптов

### Linux/Mac

Сделайте исполняемыми:

```bash
chmod +x run.sh
chmod +x update.sh
chmod +x update-smart.sh
chmod +x docker-rebuild.sh
```

### Windows

Скрипты `.bat` исполняемы по умолчанию.

---

## 📖 Использование

### Разработка (локально)

```bash
# Первый запуск
./run.sh

# Внесли изменения в код
# Просто перезапустите
Ctrl+C
./run.sh
```

### Продакшн (сервер)

```bash
# Обычное обновление
./update.sh

# Критичное обновление
./update-smart.sh

# Проблемы с Docker
./docker-rebuild.sh
```

---

## 🔄 Автоматические обновления

### Настройка cron

```bash
# Редактируем crontab
crontab -e

# Добавляем автообновление (каждую ночь в 3:00)
0 3 * * * cd /opt/social_post_generator && ./update-smart.sh >> /var/log/social-post-update.log 2>&1
```

### Проверка логов автообновления

```bash
tail -f /var/log/social-post-update.log
```

---

## 🐛 Troubleshooting скриптов

### Ошибка: "Permission denied"

```bash
# Дайте права на выполнение
chmod +x update.sh

# Или запустите через bash
bash update.sh
```

### Ошибка: "Docker not found"

```bash
# Проверьте что Docker установлен
docker --version

# Запустите Docker Desktop (Windows)
# или
sudo systemctl start docker  # Linux
```

### Скрипт не находит git

```bash
# Установите git
sudo apt install git  # Linux
# или скачайте с https://git-scm.com/  # Windows
```

---

## 📊 Логирование

### Сохранение вывода скрипта

```bash
# Linux/Mac
./update.sh 2>&1 | tee logs/update_$(date +%Y%m%d_%H%M%S).log

# Просмотр логов обновлений
ls -lt logs/update_*.log
```

### Debug режим

```bash
# Linux/Mac - добавьте в начало скрипта
set -x  # Показывать все команды

# Или запустите с bash -x
bash -x update.sh
```

---

## ✅ Рекомендации

### Для продакшн сервера:

1. ✅ Используйте `update-smart.sh`
2. ✅ Настройте автообновление через cron
3. ✅ Логируйте все обновления
4. ✅ Держите backup за 7 дней
5. ✅ Тестируйте на staging

### Для разработки:

1. ✅ Используйте `run.sh` для быстрого запуска
2. ✅ Используйте `update.sh` для обновлений
3. ✅ Используйте `docker-rebuild.sh` при проблемах

---

**Скрипты упрощают работу! Используйте их! 🚀**

