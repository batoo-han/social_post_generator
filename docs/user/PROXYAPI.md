# 📡 Использование ProxyAPI.ru

## Что такое ProxyAPI?

**ProxyAPI.ru** - это сервис для доступа к API OpenAI из России без необходимости использовать VPN.

Основные преимущества:
- ✅ Работает из России без VPN
- ✅ Оплата в рублях
- ✅ Стабильная работа без блокировок
- ✅ Совместимость с официальным SDK OpenAI
- ✅ Доступ к новейшим моделям GPT

🔗 **Сайт:** https://proxyapi.ru/  
📚 **Документация:** https://proxyapi.ru/docs/openai-text-generation

---

## Получение API ключа

1. Перейдите на https://proxyapi.ru/
2. Зарегистрируйтесь или войдите
3. Пополните баланс (оплата в рублях)
4. Получите API ключ в личном кабинете
5. Скопируйте ключ

---

## Настройка в проекте

### 1. Создайте .env файл

```bash
cp .env.example .env
```

### 2. Укажите ваш ключ ProxyAPI

Откройте файл `.env` и замените:

```ini
# Было
OPENAI_API_KEY=your_proxyapi_key_here

# Стало (ваш реальный ключ)
OPENAI_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxx
```

### 3. Проверьте настройки

Base URL должен быть:
```ini
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
```

**НЕ ИЗМЕНЯЙТЕ** этот параметр! Это эндпоинт ProxyAPI.

---

## Доступные модели

ProxyAPI поддерживает следующие модели OpenAI:

| Модель | Описание | Рекомендуется для |
|--------|----------|-------------------|
| **gpt-4o** | Самая мощная и быстрая модель | Продакшн, высокое качество |
| **gpt-4.1-mini** | Быстрая и экономичная | Разработка, тесты |
| **gpt-5-mini** | Новейшая мини-модель | Экономия, простые задачи |
| **gpt-4o-mini** | Облегченная версия gpt-4o | Быстрая генерация |
| **gpt-4-turbo** | Турбо версия GPT-4 | Сложные задачи |

### Выбор модели

В `.env` файле:

```ini
# Рекомендуется для продакшна (баланс качества и скорости)
OPENAI_MODEL=gpt-4o

# Для экономии средств
OPENAI_MODEL=gpt-4.1-mini

# Для максимального качества
OPENAI_MODEL=gpt-4-turbo
```

---

## Особенности ProxyAPI

### ⚠️ Важно знать

**ProxyAPI использует упрощенный API:**

```python
# ProxyAPI (наш проект)
response = client.responses.create(
    model="gpt-4o",
    input="Ваш промпт"
)

# НЕ стандартный OpenAI API
# response = client.chat.completions.create(
#     model="gpt-4o",
#     messages=[...],
#     temperature=0.8,  # НЕ поддерживается!
#     max_tokens=500    # НЕ поддерживается!
# )
```

### Неподдерживаемые параметры

ProxyAPI **НЕ** поддерживает:
- ❌ `temperature` - креативность генерации
- ❌ `max_tokens` - максимальное количество токенов
- ❌ `top_p` - nucleus sampling
- ❌ `frequency_penalty` - штраф за повторения
- ❌ `presence_penalty` - штраф за присутствие

Модель генерирует текст с **автоматическими** настройками.

### Как это работает в нашем проекте

Наш модуль `openai_module.py` автоматически:
1. Объединяет system_prompt с основным промптом
2. Отправляет через `responses.create()`
3. Не использует unsupported параметры

**Вам не нужно ничего менять** - все работает прозрачно!

---

## Стоимость использования

Стоимость зависит от модели и количества токенов.

### Примерные цены (проверяйте актуальные на сайте):

- **gpt-4o**: ~5-10₽ за 1000 токенов
- **gpt-4.1-mini**: ~1-2₽ за 1000 токенов
- **gpt-5-mini**: ~2-3₽ за 1000 токенов

### Средний расход на 1 пост:

- Входящий текст: ~500-1000 токенов
- Выходящий пост: ~200-300 токенов
- **Итого**: ~700-1300 токенов (5-15₽ с gpt-4o)

### Рекомендации по экономии:

1. **Используйте gpt-4.1-mini** для разработки и тестов
2. **Ограничьте длину входного текста** (в agent.py уже сделано - max 3000 символов)
3. **Используйте gpt-4o** только в продакшне

---

## Примеры кода

### Прямое использование (для понимания)

```python
from openai import OpenAI

client = OpenAI(
    api_key="ваш_ключ_proxyapi",
    base_url="https://api.proxyapi.ru/openai/v1",
)

# Генерация текста
response = client.responses.create(
    model="gpt-4o",
    input="Напиши короткий пост о Python"
)

print(response.choices[0].message.content)
```

### В нашем проекте (уже реализовано)

```python
from openai_module import get_openai_client

# Получаем клиент (автоматически настроен на ProxyAPI)
client = get_openai_client()

# Генерируем текст
text = client.generate_text(
    prompt="Напиши пост о Python",
    system_prompt="Ты автор постов в соцсетях"
)

print(text)
```

---

## Проверка работоспособности

### Тест 1: Проверка модуля

```bash
python openai_module.py
```

Ожидаемый вывод:
```
🧪 Тестирование ProxyAPI модуля...
✅ ProxyAPI клиент создан: модель=gpt-4o
📡 Base URL: https://api.proxyapi.ru/openai/v1
✅ ProxyAPI доступен
...
```

### Тест 2: Проверка через health check

```bash
curl http://localhost:8082/api/health
```

Должно вернуть:
```json
{
  "status": "healthy",
  "checks": {
    "openai": {
      "status": "available"
    }
  }
}
```

### Тест 3: Генерация поста

```bash
curl -X POST http://localhost:8082/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com",
    "style": "ироничный"
  }'
```

---

## Troubleshooting

### Ошибка: "OPENAI_API_KEY не установлен"

**Решение:**
1. Проверьте что `.env` файл создан
2. Убедитесь что ключ указан правильно (без пробелов)
3. Перезапустите приложение

### Ошибка: "ProxyAPI недоступен"

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь что `OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1`
3. Проверьте баланс на https://proxyapi.ru/

### Ошибка: "Rate limit exceeded"

**Решение:**
1. Превышен лимит запросов к ProxyAPI
2. Подождите несколько минут
3. Проверьте ваш тарифный план

### Ошибка: "Insufficient balance"

**Решение:**
1. Пополните баланс на https://proxyapi.ru/
2. Проверьте что оплата прошла

---

## Сравнение: ProxyAPI vs Официальный OpenAI

| Характеристика | ProxyAPI | Официальный OpenAI |
|----------------|----------|-------------------|
| Доступ из России | ✅ Без VPN | ❌ Требуется VPN |
| Оплата | ✅ Рубли | ❌ Только карты других стран |
| API совместимость | ⚠️ Упрощенный | ✅ Полный |
| temperature/max_tokens | ❌ Не поддерживается | ✅ Поддерживается |
| Модели | ✅ Все основные | ✅ Все |
| Скорость | ✅ Быстро | ✅ Быстро |
| Стабильность | ✅ Высокая | ⚠️ Блокировки в РФ |

---

## Альтернативы

Если ProxyAPI не подходит, можно использовать:

1. **Официальный OpenAI** - через VPN + зарубежную карту
2. **Другие прокси-сервисы** - нужно изменить `base_url`
3. **Локальные LLM** - например Llama через Ollama (бесплатно, но качество ниже)

Для смены на другой сервис - измените `OPENAI_BASE_URL` в `.env`.

---

## Полезные ссылки

- 🌐 **ProxyAPI сайт**: https://proxyapi.ru/
- 📚 **Документация**: https://proxyapi.ru/docs/openai-text-generation
- 💰 **Тарифы**: https://proxyapi.ru/ (раздел тарифы)
- 📞 **Поддержка ProxyAPI**: support@proxyapi.ru

---

## FAQ

**Q: Можно ли использовать официальный OpenAI API?**  
A: Да, но нужен VPN и зарубежная карта. Измените `OPENAI_BASE_URL` на `https://api.openai.com/v1`.

**Q: Почему не работает temperature?**  
A: ProxyAPI использует упрощенный API без этого параметра. Модель генерирует с автоматическими настройками.

**Q: Какую модель выбрать?**  
A: Для продакшна - `gpt-4o`, для разработки - `gpt-4.1-mini`.

**Q: Сколько стоит генерация одного поста?**  
A: Примерно 5-15₽ с gpt-4o, 1-3₽ с gpt-4.1-mini.

**Q: Безопасно ли использовать ProxyAPI?**  
A: Да, это легальный сервис. Но не передавайте конфиденциальные данные.

---

**Готово! Используйте ProxyAPI.ru для доступа к OpenAI из России! 🚀**

