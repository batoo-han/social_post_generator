# API Документация Social Post Generator

## Базовая информация

**Base URL:** `http://localhost:8082`  
**Content-Type:** `application/json`  
**Версия API:** 1.0

## Endpoints

### 1. Генерация поста

Генерирует пост для социальных сетей на основе контента веб-страницы.

**Endpoint:** `POST /api/generate`

#### Запрос

**Headers:**
```
Content-Type: application/json
```

**Body:**
```json
{
  "url": "https://example.com",
  "style": "ироничный"
}
```

**Параметры:**

| Поле | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| url | string | Да | URL веб-страницы для анализа. Должен начинаться с http:// или https:// |
| style | string | Нет | Стиль поста. По умолчанию: "ироничный". См. список доступных стилей в `/api/styles` |

**Валидация URL:**
- Должен быть валидным URL
- Протокол: http или https
- Максимальная длина: 2000 символов

#### Успешный ответ

**Код:** `200 OK`

**Body:**
```json
{
  "success": true,
  "post": "🎯 Python - это не просто язык программирования, это стиль жизни! Где ещё вы найдёте настолько элегантный способ заставить компьютер делать за вас всю работу? Впрочем, между нами говоря, большую часть времени мы тратим на поиск точки с запятой, которой там... не должно быть 😅",
  "length": 287,
  "style": "ироничный",
  "url": "https://example.com",
  "timestamp": "2025-11-01T12:34:56.789Z"
}
```

**Поля ответа:**

| Поле | Тип | Описание |
|------|-----|----------|
| success | boolean | Статус выполнения запроса |
| post | string | Сгенерированный пост (до 800 символов) |
| length | integer | Длина поста в символах |
| style | string | Использованный стиль |
| url | string | Обработанный URL |
| timestamp | string | Время генерации (ISO 8601) |

#### Ответы с ошибками

##### 1. Ошибка валидации

**Код:** `422 Unprocessable Entity`

```json
{
  "success": false,
  "error": "Некорректный URL. Пожалуйста, укажите полный URL с протоколом (http:// или https://)",
  "error_code": "VALIDATION_ERROR",
  "details": {
    "field": "url",
    "value": "example.com"
  }
}
```

##### 2. Ошибка загрузки страницы

**Код:** `400 Bad Request`

```json
{
  "success": false,
  "error": "Не удалось загрузить страницу. Проверьте правильность URL и доступность сайта.",
  "error_code": "URL_FETCH_ERROR",
  "url": "https://example.com"
}
```

**Возможные причины:**
- Сайт недоступен
- Таймаут соединения
- DNS ошибка
- Блокировка доступа

##### 3. Ошибка извлечения текста

**Код:** `400 Bad Request`

```json
{
  "success": false,
  "error": "На странице не найдено достаточно текста для генерации поста. Попробуйте другой URL.",
  "error_code": "TEXT_EXTRACTION_ERROR",
  "url": "https://example.com"
}
```

**Возможные причины:**
- Страница пустая
- Только изображения
- JavaScript-heavy сайт
- Требуется авторизация

##### 4. Ошибка OpenAI API

**Код:** `500 Internal Server Error`

```json
{
  "success": false,
  "error": "Временные проблемы с генерацией. Мы уже работаем над решением. Попробуйте позже.",
  "error_code": "OPENAI_ERROR"
}
```

**Возможные причины:**
- Превышен rate limit OpenAI
- Ошибка API ключа
- Проблемы с proxyapi
- Таймаут генерации

##### 5. Общая ошибка сервера

**Код:** `500 Internal Server Error`

```json
{
  "success": false,
  "error": "Произошла непредвиденная ошибка. Администратор уже уведомлен.",
  "error_code": "INTERNAL_ERROR",
  "timestamp": "2025-11-01T12:34:56.789Z"
}
```

#### Примеры использования

**cURL:**
```bash
curl -X POST http://localhost:8082/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://python.org",
    "style": "ироничный"
  }'
```

**JavaScript (Fetch API):**
```javascript
const response = await fetch('http://localhost:8082/api/generate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    url: 'https://python.org',
    style: 'ироничный'
  })
});

const data = await response.json();
if (data.success) {
  console.log('Пост:', data.post);
} else {
  console.error('Ошибка:', data.error);
}
```

**Python (requests):**
```python
import requests

response = requests.post(
    'http://localhost:8082/api/generate',
    json={
        'url': 'https://python.org',
        'style': 'ироничный'
    }
)

data = response.json()
if data['success']:
    print(f"Пост: {data['post']}")
else:
    print(f"Ошибка: {data['error']}")
```

---

### 2. Список доступных стилей

Возвращает список всех поддерживаемых стилей генерации постов.

**Endpoint:** `GET /api/styles`

#### Запрос

Параметры не требуются.

#### Успешный ответ

**Код:** `200 OK`

**Body:**
```json
{
  "success": true,
  "styles": [
    {
      "id": "ironic",
      "name": "Ироничный",
      "description": "Легкая ирония и самоирония, умный юмор",
      "emoji": "😏"
    },
    {
      "id": "professional",
      "name": "Профессиональный",
      "description": "Деловой и информативный стиль",
      "emoji": "💼"
    },
    {
      "id": "motivational",
      "name": "Мотивационный",
      "description": "Вдохновляющий и побуждающий к действию",
      "emoji": "🚀"
    },
    {
      "id": "humorous",
      "name": "Юмористический",
      "description": "Веселый и развлекательный контент",
      "emoji": "😄"
    },
    {
      "id": "educational",
      "name": "Образовательный",
      "description": "Обучающий контент с полезными фактами",
      "emoji": "📚"
    },
    {
      "id": "emotional",
      "name": "Эмоциональный",
      "description": "Трогательный и чувственный стиль",
      "emoji": "❤️"
    }
  ],
  "default": "ironic"
}
```

#### Пример использования

**cURL:**
```bash
curl http://localhost:8082/api/styles
```

**JavaScript:**
```javascript
const response = await fetch('http://localhost:8082/api/styles');
const data = await response.json();
console.log('Доступные стили:', data.styles);
```

---

### 3. Проверка здоровья сервиса

Проверяет работоспособность сервиса и его зависимостей.

**Endpoint:** `GET /api/health`

#### Запрос

Параметры не требуются.

#### Успешный ответ

**Код:** `200 OK`

**Body:**
```json
{
  "status": "healthy",
  "timestamp": "2025-11-01T12:34:56.789Z",
  "version": "1.0.0",
  "checks": {
    "openai": {
      "status": "available",
      "latency_ms": 234
    },
    "memory": {
      "used_mb": 128,
      "available_mb": 896,
      "percent": 12.5
    }
  }
}
```

#### Ответ при проблемах

**Код:** `503 Service Unavailable`

```json
{
  "status": "unhealthy",
  "timestamp": "2025-11-01T12:34:56.789Z",
  "version": "1.0.0",
  "checks": {
    "openai": {
      "status": "unavailable",
      "error": "Connection timeout"
    },
    "memory": {
      "used_mb": 950,
      "available_mb": 74,
      "percent": 92.8
    }
  }
}
```

#### Пример использования

**cURL:**
```bash
curl http://localhost:8082/api/health
```

---

## Коды ошибок

| Код | Описание | Действия пользователя |
|-----|----------|----------------------|
| VALIDATION_ERROR | Некорректные входные данные | Проверьте формат URL и стиль |
| URL_FETCH_ERROR | Ошибка загрузки страницы | Проверьте URL и доступность сайта |
| TEXT_EXTRACTION_ERROR | Не удалось извлечь текст | Попробуйте другой URL с большим количеством текста |
| OPENAI_ERROR | Ошибка генерации AI | Попробуйте позже, проблема решается |
| RATE_LIMIT_ERROR | Превышен лимит запросов | Подождите немного перед следующим запросом |
| INTERNAL_ERROR | Внутренняя ошибка сервера | Администратор уведомлен, попробуйте позже |

## Rate Limiting

**Лимиты:**
- 10 запросов в минуту с одного IP
- 100 запросов в час с одного IP

**Заголовки ответа:**
```
X-RateLimit-Limit: 10
X-RateLimit-Remaining: 7
X-RateLimit-Reset: 1698840000
```

**Ответ при превышении:**
```json
{
  "success": false,
  "error": "Превышен лимит запросов. Пожалуйста, подождите перед следующей попыткой.",
  "error_code": "RATE_LIMIT_ERROR",
  "retry_after": 45
}
```

## CORS

**Разрешенные источники:**
- `http://localhost:8082`
- `http://127.0.0.1:8082`
- Настраивается в `.env` через `ALLOWED_ORIGINS`

**Разрешенные методы:**
- GET
- POST
- OPTIONS

**Разрешенные заголовки:**
- Content-Type
- Authorization (для будущих версий)

## Версионирование

Текущая версия: **v1**

API версионируется через URL path (например, `/api/v1/generate`).  
В текущей версии префикс `/api/v1` опциональен для обратной совместимости.

## Changelog

### v1.0.0 (2025-11-01)
- Первый релиз
- Endpoint генерации постов
- Endpoint списка стилей
- Health check endpoint
- 6 стилей генерации
- Rate limiting
- Обработка ошибок

## Поддержка

Для вопросов и проблем:
- GitHub Issues: [repository]/issues
- Email: support@example.com
- Документация: /docs

